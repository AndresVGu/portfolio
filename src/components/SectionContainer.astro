---
const { class: className, id } = Astro.props;
---

<section
  id={id}
  data-section={id}
  class={`section scroll-mt-24 w-full mx-auto px-4 sm:px-6
          max-w-2xl md:max-w-3xl lg:max-w-4xl xl:max-w-5xl
          ${className ?? ''}`}
>
  <slot />
</section>
