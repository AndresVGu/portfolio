---
import ThemeToggle from './ThemeToggle.astro';
import { getI18N, getLocalePath } from '@/i18n';

interface Props {
  currentLocale?: string;
}

const { currentLocale = 'en' } = Astro.props;
const i18n = getI18N({ currentLocale });
const base = getLocalePath(currentLocale);

// Build locale-aware language hrefs for the current project page
const pathname = Astro.url.pathname;
const projectMatch = pathname.match(/(?:\/(?:es|fr))?\/projects\/([^/]+)/);
const slug = projectMatch?.[1] ?? '';

const languages = [
  { label: 'EN', locale: 'en', href: slug ? `/projects/${slug}`    : '/'    },
  { label: 'ES', locale: 'es', href: slug ? `/es/projects/${slug}` : '/es/' },
  { label: 'FR', locale: 'fr', href: slug ? `/fr/projects/${slug}` : '/fr/' },
];

const backHref = `${base}/#projects`;
---

<!-- Same structure as main Header -->
<header
  id="site-header"
  class="fixed top-0 left-0 right-0 z-40 flex items-center justify-between
         px-4 sm:px-6 h-14 sm:h-16
         transition-all duration-300"
>
  <!-- ── Logo / brand (mobile only) ──────────────────────────────────── -->
  <a
    data-back-to-portfolio
    href={backHref}
    class="md:hidden font-bold text-sm text-gray-800 dark:text-gray-100 tracking-wide select-none
           flex items-center gap-1"
  >
    <svg xmlns="http://www.w3.org/2000/svg" class="size-4" fill="none"
         viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
      <path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7" />
    </svg>
    AV<span class="text-yellow-500">.</span>
  </a>

  <!-- ── Desktop pill nav (centered, same style as main) ─────────────── -->
  <nav
    id="desktop-nav"
    class="hidden md:flex items-center gap-1 lg:gap-2
           mx-auto
           px-3 py-1.5 rounded-full
           text-sm lg:text-base"
  >
    <!-- Back button -->
    <a
      data-back-to-portfolio-desktop
      href={backHref}
      class="nav-link px-3 py-1.5 rounded-full font-medium transition-colors duration-200
             text-gray-600 dark:text-gray-300
             hover:text-gray-900 dark:hover:text-white
             hover:bg-gray-100 dark:hover:bg-gray-800
             flex items-center gap-1.5"
    >
      <svg xmlns="http://www.w3.org/2000/svg" class="size-4" fill="none"
           viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
        <path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7" />
      </svg>
      {i18n.CAROUSEL.backToPortfolio.replace('← ', '')}
    </a>

    <!-- Divider -->
    <span class="w-px h-5 bg-gray-200 dark:bg-gray-700 mx-1"></span>

    <!-- Language switcher -->
    <div class="relative" id="lang-wrapper-desktop">
      <button
        id="dropdown-language"
        class="flex items-center gap-1 px-3 py-1.5 rounded-full font-semibold transition-colors
               text-gray-600 dark:text-gray-300
               hover:text-gray-900 dark:hover:text-white
               hover:bg-gray-100 dark:hover:bg-gray-800"
        aria-label="Select language"
      >
        {currentLocale.toUpperCase()}
        <svg class="w-3 h-3" fill="none" viewBox="0 0 10 6">
          <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"
                stroke-width="2" d="m1 1 4 4 4-4"/>
        </svg>
      </button>
      <div
        id="language-menu"
        class="absolute hidden top-10 right-0 text-sm p-1 min-w-[5rem] rounded-xl
               border border-gray-200 dark:border-gray-700
               bg-white dark:bg-gray-900
               shadow-lg backdrop-blur-md z-50"
      >
        <ul>
          {languages.map((lang) => (
            <li>
              <a
                href={lang.href}
                class={`block px-3 py-2 rounded-lg cursor-pointer transition-colors
                        text-gray-700 dark:text-gray-200
                        hover:bg-gray-100 dark:hover:bg-gray-800
                        ${currentLocale === lang.locale
                          ? 'font-bold text-yellow-600 dark:text-yellow-400 bg-yellow-50 dark:bg-yellow-900/20'
                          : ''}`}
              >
                {lang.label}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>

    <ThemeToggle />
  </nav>

  <!-- ── Mobile right controls ────────────────────────────────────────── -->
  <div class="md:hidden flex items-center gap-2">
    <ThemeToggle />
  </div>
</header>

<script>
  import { initLangDropdown, initHeaderScroll } from '@/scripts/header-shared';

  document.addEventListener('astro:page-load', () => {
    // Update back-link hrefs at runtime to match current document lang
    const lang   = document.documentElement.lang ?? 'en';
    const prefix = lang === 'es' ? '/es' : lang === 'fr' ? '/fr' : '';

    document.querySelectorAll<HTMLAnchorElement>('[data-back-to-portfolio], [data-back-to-portfolio-desktop]').forEach(link => {
      link.href = `${prefix}/#projects`;
    });

    initLangDropdown();
    initHeaderScroll();
  });
</script>

<style>
  @import '@/styles/dropdown.css';
</style>
